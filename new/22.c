#include <stdio.h>
#include <string.h>

int main()
{
    char S[1001], T[1001];
    int countS[26] = {0}, countT[26] = {0};

    // Input strings
    scanf("%s", S);
    scanf("%s", T);

    return 0;
}
